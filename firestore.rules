rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Untuk development, izinkan semua (nanti batasi untuk production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}